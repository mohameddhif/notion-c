import ProjectsList from '../components/ProjectsList';
import { sampleProjects } from '../constants/index';

const Projects = () => {
  return <ProjectsList projects={sampleProjects} />;
};

export default Projects;
